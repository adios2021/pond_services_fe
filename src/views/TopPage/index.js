import { connect } from 'react-redux';
// import { Dashboard } from '../../actions/AuthPage';
import TopPage from './TopPage';

const mapsDispatchToProps = (dispatch) => ({});

export default connect(null, mapsDispatchToProps)(TopPage)